# 최종 보고서

## 1. 프로그램 주제와 목적

내 프로그램의 주제: 공간에 따른 조명 밝기와 개수 추천 프로그램

이 프로그램을 만든 이유: 나는 건축에 관심이 있고 건축에서 공간의 분위기를 조성하는데 가장 주요한 요소가 조명이기 떄문이다.


---

## 2. 문제 분해 및 모델링

입력
사용자가 입력하거나 프로그램이 사용할 데이터는 무엇인가?

사용자에게 콘솔 창(input())을 통해 다음 두 가지 정보를 입력받습니다.

공간 선택: 추천받고자 하는 건축 공간의 종류 (예: 거실, 침실, 공부방, 부엌)

방의 면적: 공간의 크기  (예: 15, 24 등 정수나 실수형 데이터)

처리
입력된 데이터를 어떤 규칙이나 알고리즘으로 처리할 것인가?

데이터 탐색(반복&조건): 2차원 리스트(lighting_data)를 for문으로 순회하면서, 사용자가 입력한 공간 이름이 리스트의 몇 번째 행에 있는지 if문으로 비교하여 일치하는 적정 조도 기준(Lux) 값을 찾아냅니다.

필요 총 밝기 계산: 건축 조도 설계 공식을 활용하여 공간에 필요한 총 밝기를 계산합니다.

방의 면적 공간별 조도 기준(Lux) 형광등 개수 계산: 시중에서 많이 쓰는 표준 일자형 형광등 1개당 밝기(예: 2000루멘)를 기준으로 나누어, 필요한 최종 형광등 개수를 올림 또는 정수형태로 계산합니다.

출력
프로그램이 최종적으로 무엇을 출력하는가?

선택한 공간의 조도 기준 안내: "선택하신 [공부방]의 적정 조도는 [500] Lux입니다."

최종 추천 결과 출력: "입력하신 면적을 바탕으로 계산한 결과, 이 공간에는 약 [3]개의 일자형 형광등(2000lm 기준)이 필요합니다."


---

## 3. 사용한 2차원 리스트 설명

2차원 리스트 이름: space_lux_list

한 행의 의미: '건축 공간 정보'

각 열의 의미: 0번 열은 '공간의 이름', 1번 열은 '그 공간의 적정 조도 기준값(Lux)'을 의미합니다.

| 열 번호 | 의미 | 실제 예시 |
0번 열	추천받고자 하는 건축 공간의 종류 (방 이름)	"침실", "거실", "공부방", "부엌"
1번 열	실내 건축 설계 기준에 따른 공간별 적정 조도 (Lux)	150, 300, 500, 250

이 2차원 리스트를 사용한 이유:방 이름(문자열)과 적정 조도(숫자)처럼 서로 연관된 데이터를 표 형태로 묶어서 편리하게 관리하기 위해 사용했습니다.

이렇게 짜두면 나중에 새로운 방(예: 화장실)이 추가되더라도, 전체 코드를 고칠 필요 없이 리스트에 데이터만 한 줄 추가하면 되기 때문에 관리가 매우 쉽습니다.


---

## 4. 함수 설명

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |
show_intro()
의미: 프로그램의 제목과 안내문을 화면에 보여주는 함수
get_user_input()
의미: 사용자에게 설계할 '공간 이름'과 '방 면적'을 입력받는 함수
find_lux()
의미: 2차원 리스트(표)에서 해당 공간의 적정 밝기 기준(Lux)을 찾아내는 함수
print_result()
의미: 면적과 밝기를 곱해 필요한 '최종 형광등 개수'를 계산하고 결과를 보여주는 함수
---


가장 중요한 함수 하나를 골라 자세히 설명하세요.

함수 이름:find_lux()

설명:리스트에서 사용자가 입력한 공간의 밝기(Lux)를 찾는다. 이 프로그램의 핵심은 사용자가 각 원하는 공간에 필요한 밝기 강도 이기 때문에 이 함수가 가장 중요하다.


---

## 5. 조건문과 반복문 활용

### 조건문 사용 부분

어떤 조건을 검사했는가?
내 계획: 사용자가 입력한 공간이 리스트에 있는지 비교할 때
리스트에 없는 잘못된 공간 이름을 입력했을 때 (예외 처리)
전구 개수를 계산할 때 소수점 올림 처리

### 반복문 사용 부분

무엇을 반복 처리했는가?
내 계획:2차원 리스트 데이터를 한 줄씩 처음부터 끝까지 읽을 때

이유: 공간별 정보가 들어있는 2차원 리스트를 for문으로 순회하면서, 우리가 원하는 방의 정보가 나올 때까지 데이터를 차례대로 하나씩 꺼내서 확인(탐색)합니다.

### 조건문과 반복문이 함께 작동한 부분
(41~44번 줄)
반복문: - 2차원 리스트(표)의 첫 번째 줄(침실)부터 마지막 줄(부엌)까지 위에서 아래로 한 행씩 차례대로 꺼내며 내려갑니다.

조건문: - 꺼내온 그 줄의 방 이름이 사용자가 입력한 이름(예: "공부방")과 똑같은지 매번 멈춰서 확인합니다.

---

## 6. 실행 결과

대표 실행 결과를 붙여넣으세요.

```text
[추천 결과]
- 선택하신 공간: 거실
- 공간의 적정 조도: 300 Lux
- 입력하신 면적: 15.0 m²
- 이 공간에 필요한 총 밝기: 4500.0 lm
----------------------------------------
💡 추천 결과: 일자형 형광등(2000lm) 총 **3개**가 필요합니다.
```

결과 분석:거실 15 m²에는 일자형 형광등(2000lm) 총 3개가 필요하다


---

## 7. 테스트와 오류 수정

### 테스트 1: 정상 사례

입력:거실, 부엌 등 각 공간의 면적 (예:15, 12)

예상 결과: 각 공간에 필요한 밝기와 그에따른 형광등 추천

실제 결과:각 공간에 필요한 밝기와 그에따른 형광등 추천


### 테스트 2: 예외 또는 경계 사례

입력: 방, 화장실

예상 결과: 등록 되지않은 공간이라고 뜬다

실제 결과:❌ 오류: 등록되지 않은 공간입니다.
목록에 있는 공간(침실, 거실, 공부방, 부엌)을 정확히 입력해 주세요.


### 발견한 오류와 수정한 방법

조건문으로 예외 상황을 만들어서 필요한 출력 값을 유도한다.
---

## 8. Gemini 활용 내용 요약

Gemini에게 도움받은 부분:
1. 코딩 짜기
2. 2차원 리스트 짜기 
3. 코드 내용 설명 하기


Gemini가 제안했지만 내가 수정하거나 사용하지 않은 부분: 필요하지 않는 출력값은 삭제하였다


최종 코드에서 내가 직접 판단하거나 수정한 부분: 오류와 너무긴 함수 이름 필요하지 않는 출력값 등을 정리하였다


---

## 9. 한계와 개선 방안

현재 프로그램의 한계:현재 프로그램의 한계:벽지 색상이나 창문 크기 미반영: 실제 실내 건축에서는 방의 면적뿐만 아니라 벽지의 색상(빛 반사율)이나 창문의 크기(자연광 유입량)에 따라서도 필요한 조명이 달라지는데, 현재 프로그램은 순수하게 면적과 공간 종류만 고려하여 계산합니다.고정된 전등 종류: 무조건 2,000lm 밝기의 일자형 형광등 개수만 계산해 주기 때문에, 사용자가 원형 LED 전구나 은은한 무드등을 달고 싶을 때는 개수를 예측하기 어렵습니다.

다시 개선한다면 추가하고 싶은 기능:다양한 조명 선택 기능: 사용자가 형광등, 다운라이트, LED 전구, 무드등 중 원하는 조명 종류를 직접 선택하면, 해당 조명의 표준 루멘(lm) 값을 적용하여 개수를 다르게 계산해 주는 기능을 추가하고 싶습니다.평수를 제곱미터로 자동 환산하는 기능: 사용자가 10평, 24평처럼 평수 단위로 입력해도 프로그램 내부에서 자동으로 제곱미터($m^2$)로 환산하여 계산해 주는 편의 기능을 추가하고 싶습니다.



---

## 10. 코드인터뷰 준비

다음 질문에 답할 수 있도록 준비하세요.

1. 내 프로그램의 입력-처리-출력은 무엇인가?
2. 2차원 리스트에서 한 행과 각 열은 무엇을 의미하는가?
3. 가장 중요한 함수는 무엇이고 어떤 역할을 하는가?
4. 조건문과 반복문은 어디에서 사용되었는가?
5. Gemini가 도와준 부분과 내가 직접 수정한 부분은 무엇인가?

1. 내 프로그램의 입력-처리-출력은 무엇인가?
입력: 사용자에게 콘솔 창으로 입력받는 [공간 선택(방 이름)]과 [방의 면적] 데이터입니다.

처리: 2차원 리스트에서 공간에 맞는 조도(Lux)를 찾는 탐색 처리와, 면적을 곱해 형광등 개수를 구하고 나머지를 올림 처리하는 연산 처리입니다.

출력: 선택한 공간의 조도 기준 안내와 설계에 필요한 [최종 일자형 형광등 개수]를 화면에 출력합니다.

2. 2차원 리스트에서 한 행과 각 열은 무엇을 의미하는가?
한 행(줄): 하나의 건축 공간에 대한 세트 정보를 의미합니다.

0번 열: 추천받고자 하는 건축 공간의 종류(방 이름)를 뜻합니다.

1번 열: 실내 건축 설계 기준에 따른 공간별 적정 조도(Lux) 값을 뜻합니다.

3. 가장 중요한 함수는 무엇이고 어떤 역할을 하는가?
최종 결과를 계산하고 출력하는 print_result() 함수가 가장 중요합니다.

이 함수는 입력받은 면적과 조도를 활용해 실내 건축 조도 공식을 계산하고, 형광등 밝기(2,000lm)로 나눈 뒤 나머지가 생기면 1개를 더 더해주는 핵심 올림 알고리즘을 수행하기 때문입니다.

4. 조건문과 반복문은 어디에서 사용되었는가?
find_lux() 함수 내부에서 함께 사용되었습니다. for문(반복문)으로 2차원 리스트를 맨 위부터 한 행씩 훑어 내리는 와중에, if문(조건문)을 사용해 사용자가 입력한 방 이름과 일치하는지 돋보기처럼 검사하는 데이터 탐색 알고리즘에 쓰였습니다.

또한 print_result() 함수 내부에서 없는 공간을 걸러내는 예외 처리와, 형광등 개수를 올림 처리하는 조건문(if)으로도 사용되었습니다.

5. Gemini가 도와준 부분과 내가 직접 수정한 부분은 무엇인가?
Gemini가 도와준 부분: 내 진로인 건축학과에 맞춰 '적정 조도(Lux)' 개념을 활용한 미니 프로젝트 주제를 제안해 주었고, 학교의 함수 분리 템플릿 구조에 맞게 파이썬 코드를 설계해 주었습니다.

내가 직접 수정한 부분: AI는 처음에 일반 전구(800lm) 기준으로 코드를 짜주었지만, 실제 건축 인테리어 환경에서는 거실이나 공부방에 일자형 형광등을 더 많이 쓴다는 생각이 들어 내가 주도적으로 형광등(2,000lm) 기준으로 수식을 변경하고 올림 처리 알고리즘을 보완해 달라고 판단하여 수정했습니다.